#ifndef FUZZYQUERY_H
#define FUZZYQUERY_H


class FuzzyQuery
{
    public:
        FuzzyQuery();
        virtual ~FuzzyQuery();

        float Query(float,float,int);

    protected:

    private:
};

#endif // FUZZYQUERY_H
